export class Cart {
    // productID: number;
    // productName: string;
    // productDesc: string;
    userId: number;    id: number;
    title: string;    completed: boolean;

    constructor(public uId: number, public name: string, public desc: string) {}
  }
  

